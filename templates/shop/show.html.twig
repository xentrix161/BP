{% extends 'page_temps/base_temp.html.twig' %}
{% block body %}
    <h1>Shop</h1>

    <table class="table form-wrapper">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop.id }}</td>
            </tr>
            <tr>
                <th>Profit</th>
                <td>{{ shop.profit }}</td>
            </tr>
            <tr>
                <th>Order_id</th>
                <td>{{ shop.orderId }}</td>
            </tr>
        </tbody>
    </table>

    <div class="create-new">
        <a href="{{ path('shop_index') }}">back to list</a>
    </div>

{#    <a href="{{ path('shop_edit', {'id': shop.id}) }}">edit</a>#}

{#    {{ include('shop/_delete_form.html.twig') }}#}
{% endblock %}
