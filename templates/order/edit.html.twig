{% extends 'page_temps/base_temp.html.twig' %}
{% block body %}
    <h1>Upraviť objednávku</h1>

    {{ include('order/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('order_index') }}">Späť na zoznam</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
