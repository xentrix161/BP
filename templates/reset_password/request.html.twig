{% extends 'page_temps/base_temp.html.twig' %}

{% block title %}Obnovenie hesla{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

{#    {{ form_start(register_form, {'attr': {'id': 'custom_register_form',#}
{#        'class': 'form-wrapper' }}) }}#}
    {{ form_start(requestForm, {'attr': {'class': 'form-wrapper'}}) }}
    <h1 class="h3 mb-3 font-weight-normal">Obnovenie hesla</h1>

        {{ form_row(requestForm.email) }}
    <div >
            <small>
                Zadajte svoju e-mailovú adresu a my vám pošleme odkaz na obnovenie hesla.
            </small>
        </div>

        <button class="button radius">Pošlite e-mail na obnovenie hesla</button>
    {{ form_end(requestForm) }}
{% endblock %}