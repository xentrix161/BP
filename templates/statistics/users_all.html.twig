{% extends 'page_temps/base_temp.html.twig' %}

{% block body %}
    <h1>Registrovaní používatelia</h1>
    <table class="table">
        <thead>
        <tr>
            <th class="id-class">ID</th>
            <th class="name-surname">Meno</th>
            <th class="name-surname">Email</th>
            <th class="name-surname">Rola</th>
{#            <th class="name-surname">Zárobok</th>#}
{#            <th class="name-surname">Minuté</th>#}
{#            <th class="name-surname">Hodnotenie</th>#}
        </tr>
        </thead>
        <tbody>
        {% for user in listOfUsers %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.name }} {{ user.surname }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.role[0] }}</td>
{#                <td>{{ user.earning }}</td>#}
{#                <td>{{ user.expense }}</td>#}
{#                <td><b>{{ user.rating }}</b> z 5</td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="create-new">
        <a href="{{ path('statistics') }}">Späť na zoznam štatistík</a>
    </div>
{% endblock %}


