@import "general/homepage_layout.styl"
@import "general/form_layout.styl"
@import "general/article_layout.styl"
@import "general/topPanel_layout.styl"
@import "general/crude_layout.styl"
@import "general/categoryPanel_layout.styl"

//kvoli rem
:root {
  font-size 16px
}

html, body {
  color red
  font-size 1rem
  font-family 'Arial'
  background-color black
}

* {
  margin 0
  padding 0
  box-sizing border-box
}

.control-label {
  color white
}

h1
  color red

.clear-fix
  &::after
    content ""
    display block
    clear both


.float-left {
  float left
}

.float-right {
  float right
}

a
  color yellow
  text-decoration none
  img
    border none
  &:hover
    color white
    text-decoration underline

img
  width 100%
  display inline-block

ul, ol
  li
    list-style none

.btn
  border none
  &-form
    font-weight bold
    box-shadow 0 0 3pt 2pt yellow
    border-radius 17px
    margin 1.5em 0 0 0
    color black

input[type="text"], input[type="password"], textarea, select, button
  border none
  outline none
  appearance none
  &:focus
    outline none


.rate {
  float: left;
  height: 46px;
  padding: 0 10px;
}
.rate:not(:checked) > input {
  position:absolute;
  top:-9999px;
}
.rate:not(:checked) > label {
  float:right;
  width:1em;
  overflow:hidden;
  white-space:nowrap;
  cursor:pointer;
  font-size:30px;
  color:#ccc;
}
.rate:not(:checked) > label:before {
  content: '★ ';
}
.rate > input:checked ~ label {
  color: #ffc700;
}
.rate:not(:checked) > label:hover,
.rate:not(:checked) > label:hover ~ label {
  color: #deb217;
}
.rate > input:checked + label:hover,
.rate > input:checked + label:hover ~ label,
.rate > input:checked ~ label:hover,
.rate > input:checked ~ label:hover ~ label,
.rate > label:hover ~ input:checked ~ label {
  color: #c59b08;
}



/* Add a black background color to the top navigation */
.topnav
  background-color black
  overflow hidden
  a
    float left
    color yellow
    text-align center
    padding 14px 16px
    text-decoration none
    font-size 17px
    &.active
      background-color #111111
      color yellow
    &:hover
      background-color grey
      color yellow



